// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ChromaSubsampling } from "./ChromaSubsampling";
import type { Color } from "./Color";
import type { FrameRate } from "./FrameRate";
import type { Hdr } from "./Hdr";
import type { Rational } from "./Rational";

export type VideoInfo = { 
/**
 * Coded dimensions, as stored.
 */
width: number, height: number, 
/**
 * Dimensions as displayed, after rotation. What the preview must show.
 */
displayWidth: number, displayHeight: number, 
/**
 * Counter-clockwise display rotation in degrees, normalised to 0, 90,
 * 180 or 270 — the angle `ffprobe` reports from the display matrix.
 */
rotation: number, pixelFormat: string | null, bitDepth: number | null, chromaSubsampling: ChromaSubsampling | null, sampleAspectRatio: Rational | null, frameRate: FrameRate, 
/**
 * `progressive`, `tt`, `bb`, `tb`, `bt`, as FFmpeg names them.
 */
fieldOrder: string | null, hasBFrames: boolean, color: Color, 
/**
 * Present only when the stream is HDR.
 */
hdr: Hdr | null, 
/**
 * A cover image rather than a moving picture.
 */
isAttachedPicture: boolean, };
