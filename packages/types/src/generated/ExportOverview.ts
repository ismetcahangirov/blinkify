// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AudioEncoding } from "./AudioEncoding";
import type { DiskSpace } from "./DiskSpace";
import type { Reason } from "./Reason";
import type { SizeEstimate } from "./SizeEstimate";
import type { SnapOffer } from "./SnapOffer";
import type { StreamClaim } from "./StreamClaim";

/**
 * What exporting the plan to a target will do, before it is asked for.
 */
export type ExportOverview = { 
/**
 * `None` for a stream the output does not have.
 */
video: StreamClaim | null, audio: StreamClaim | null, 
/**
 * Nothing at all is re-encoded.
 */
lossless: boolean, durationSeconds: number, 
/**
 * In output order: the pictures' reasons, then the sound's.
 */
reasons: Array<Reason>, snap?: SnapOffer, size: SizeEstimate, 
/**
 * What re-encoded sound is made as, where any is.
 */
audioEncoding?: AudioEncoding, space?: DiskSpace, 
/**
 * A file is at the target already: replacing it must be confirmed.
 */
targetExists: boolean, 
/**
 * Why the export would be refused as it stands; empty when it can run.
 */
problems: Array<string>, };
