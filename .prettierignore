node_modules
dist
target
.turbo
pnpm-lock.yaml
apps/desktop/src-tauri/gen
tools/project-graph/output

# Generated by ts-rs from the Rust types. Formatting it here would make
# `pnpm types:generate` non-idempotent: the next `cargo test` rewrites these
# files unformatted, and the format gate fails on a tree nobody touched.
# Machine output is read, not styled.
packages/types/src/generated
