{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The narrowest capability set the application actually uses. CLAUDE.md section 11: the allowlist grants the narrowest scope that works, and it is easier to keep narrow than to make narrow.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
